class tick_Counter {
    /*@ specification tick_Counter {
    double init, out;
    double next, cur;
    double initstate = init;
    double nextstate, state;
    out = state;
    nextstate = state + 1;
    }@*/

}
